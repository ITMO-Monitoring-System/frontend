services:
  frontend:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_BIND_PORT:-18081}:80"
    networks:
      - default
      - backend

networks:
  backend:
    external: true
    name: backend_default
